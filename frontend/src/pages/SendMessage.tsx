import React from "react";

const SendMessage: React.FC = () => {
    return (
        <div>
            <h1>send message</h1>
            {/*loan request's homepage*/}
        </div>
    );
};

export default SendMessage;